import { Container, Typography } from '@mui/material';

const Projects = () => {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 4 }}>Projects</Typography>
    </Container>
  );
};

export default Projects; 